class Medicine {
  constructor(id, name, category, price, stock) {
    this.id = id;
    this.name = name;
    this.category = category;
    this.price = price;
    this.stock = stock;
  }

  addMedicine() {
    //add logic to add medicine
  }

  updateStock() {
    // update medicine stock
  }

  deleteMedicine() {
    //delete a medicine
  }
}

module.exports = Medicine;
